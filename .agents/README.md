# .agents/

Third-party and feature-specific skills/agents shipped by `create-eth`
(`skills/openzeppelin`, `skills/erc-721`, `skills/siwe`, etc. — read when the
task matches).

This hackathon factory's own core workflow skills (contract development,
security review, frontend web3, testing, deployment, CI, product/hackathon
execution) live in **`.claude/skills/`** instead, to keep one canonical
location rather than two copies drifting apart. Start with
[`AGENTS.md`](../AGENTS.md) at the repo root, which indexes both.
